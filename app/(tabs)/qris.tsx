import { Image, StyleSheet, Platform, View, Text } from 'react-native';
import QrisPage from '../pages/qris/qris';
import Pin from '../pages/pin/pin';

export default function QrisScreen() {
  return (
   <QrisPage/>
  // <Pin/>
  );
}

